import { Button } from '@/components/ui/button'

export default function LoadMoreComponent() {
	return (
		<div className="mt-12 text-center">
			<Button className="border-slate-600 text-slate-300 hover:bg-slate-800" variant="outline">
				Charger plus de dossards
			</Button>
		</div>
	)
}
